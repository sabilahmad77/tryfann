# Standalone TryFANN API — production dependency set.
# = the trimmed funnel runtime (requirements-local-tryfann.txt) PLUS the
# pieces a real host needs: WSGI server, static serving, Postgres driver,
# DATABASE_URL parsing. Deliberately excludes torch/tf/web3/faiss/opencv.

Django==4.2.10
djangorestframework==3.14.0
djangorestframework-simplejwt==5.5.1
PyJWT==2.10.1
django-cors-headers
channels==4.1.0
celery==5.3.6
redis==5.0.4
Pillow>=10.0.0
python-dotenv==1.0.1
environs==9.5.0
django-extensions==3.2.3
django-crontab==0.7.1
tzdata==2023.3

# --- production-only ---
gunicorn==21.2.0
whitenoise==6.6.0
dj-database-url==2.1.0
psycopg2-binary==2.9.9   # used only when DATABASE_URL is Postgres (next phase)
django-anymail[resend]==11.0.1   # Resend transactional email over HTTPS (Render blocks SMTP ports)
google-auth==2.29.0   # verify Google Sign-In ID tokens server-side
